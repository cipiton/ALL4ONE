addition = '''def show_step_output_preview(step_title: str, text: str, *, max_lines: int = 18, max_chars: int = 1400) -    print()\n    print(f\"Preview: {step_title}\")\n    preview_text = text.strip()\n    if len(preview_text)         preview_text = preview_text[:max_chars].rstrip() + \"\n... [truncated preview]\"\n    lines = preview_text.splitlines()\n    if len(lines)         preview_text = \"\n\".join(lines[:max_lines]) + \"\n... [truncated preview]\"\n    print(preview_text or \"[empty output]\")\n\n\ndef prompt_for_review_action() -    print()\n    print(\"A. Accept and continue\")\n    print(\"R. Revise this step\")\n    print(\"V. View full output\")\n    print(\"C. Cancel run\")\n    while True:\n        raw_value = input(\"Choose an action: \").strip().lower()\n        if raw_value in {\"a\", \"accept\"}:\n            return \"accept\"\n        if raw_value in {\"r\", \"revise\"}:\n            return \"revise\"\n        if raw_value in {\"v\", \"view\"}:\n            return \"view_full\"\n        if raw_value in {\"c\", \"cancel\"}:\n            return \"cancel\"\n        print(\"Choose A, R, V, or C.\")\n\n\ndef prompt_for_revision_request() - 
